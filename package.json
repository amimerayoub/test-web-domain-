{
  "name": "domainkit-api",
  "version": "1.0.0",
  "description": "DomainKit — All-in-one Domain API Suite (Vercel)",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": { "dev": "vercel dev", "deploy": "vercel --prod" },
  "devDependencies": { "vercel": "^33.0.0" }
}
